# Autoloader
The file ``autoload.json`` contains the vendor namespace prefixes and an array of corresponding base directories. It conforms to the PSR-4 standards.

# Config
The file ``config.json`` contains variables that are of global importance and are used throughout the whole application

# Container
The file ``container.json`` contains dependencies and their subdependencies and is used to inject valid objects throughout the application